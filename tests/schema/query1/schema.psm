term_expression = {
	variable = string,
	type = string,
	value = string,
	len = integer
}

term_expression = {
	variable = string,
	op = string,
	range = integer,
	cardinality = integer,
	arg = { term_expression }
}

sentence = {
	field = string,
	content = string,
	results = number,
	minweight = number
}

query = {
	feature = {{
		set = string,
		weight = number,
		sentence = sentence,
		content = term_expression,
		analyzed = term_expression
	}}
}

